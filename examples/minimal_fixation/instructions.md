# Minimal fixation

Watch the central fixation point. It will be displayed briefly three times.
No response is required.

This example uses a simulated display by default, so these instructions are
recorded in the session log and the run begins automatically.
