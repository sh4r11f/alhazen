# Per-session settings; the preparation's geometry lives in task.py, where
# it is versioned with the experiment. Anything RFMapParams declares can be
# overridden here — including the grid, for a one-off session at another
# site.
n_reps_per_cell: 3
probes_per_trial: 12
flash:
  ms: 150
isi:
  ms: 100
iti:
  ms: 400
n_display_maps: 6
